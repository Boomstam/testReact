import {Navbar} from 'react-bootstrap';

export function MenuNavbar(props) {

    return <Navbar fixed="top" className="bg-dark">
            <Navbar.Brand href="#home" className="text-white p-1">Ons café</Navbar.Brand>
        </Navbar>
}